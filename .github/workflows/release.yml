name: Release

on:
  workflow_dispatch:
    inputs:
      nextVersion:
        description: "Specify the release version in semver format v[major].[minor].[patch] e.g. v0.0.0"
        required: true

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if tag already exists
        run: |
          if git rev-parse ${{ github.event.inputs.nextVersion }} >/dev/null 2>&1; then
            echo "Tag ${{ github.event.inputs.nextVersion }} already exists."
            exit 1
          else
            echo "Tag does not exist. Building release version ${{ github.event.inputs.nextVersion }}"
          fi

      - uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build plugin
        run: npm run build

      - name: Package plugin tar.gz for ArtifactHub
        run: |
          VERSION=${{ github.event.inputs.nextVersion }}
          SEMVER=${VERSION#v}
          mkdir -p package/opencontrolplane
          cp dist/main.js package/opencontrolplane/main.js
          cp package.json package/opencontrolplane/package.json
          tar -czf headlamp-opencontrolplane-${SEMVER}.tar.gz -C package opencontrolplane
          echo "TARBALL=headlamp-opencontrolplane-${SEMVER}.tar.gz" >> $GITHUB_ENV
          echo "SEMVER=${SEMVER}" >> $GITHUB_ENV

      - name: Write artifacthub-pkg.yml for this version
        run: |
          CHECKSUM=$(sha256sum ${{ env.TARBALL }} | awk '{print $1}')
          SEMVER=${{ env.SEMVER }}
          VERSION=${{ github.event.inputs.nextVersion }}
          TEMPLATE="artifacthub/0.0.2/artifacthub-pkg.yml"
          PKGFILE="artifacthub/${SEMVER}/artifacthub-pkg.yml"
          mkdir -p "artifacthub/${SEMVER}"
          cp "$TEMPLATE" "$PKGFILE"
          [ -f "artifacthub/0.0.2/README.md" ] && cp "artifacthub/0.0.2/README.md" "artifacthub/${SEMVER}/README.md"
          sed -i "s|version: .*|version: ${SEMVER}|" "$PKGFILE"
          sed -i "s|https://github.com/openmcp-project/[^/]*/releases/download/[^/]*/[^ ]*\.tar\.gz|https://github.com/openmcp-project/opencontrolplane-headlamp-plugin/releases/download/${VERSION}/headlamp-opencontrolplane-${SEMVER}.tar.gz|g" "$PKGFILE"
          sed -i "s|sha256:.*|sha256:${CHECKSUM}|" "$PKGFILE"
          echo "PKGFILE=${PKGFILE}" >> $GITHUB_ENV

      - name: Open PR with artifacthub metadata
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ github.event.inputs.nextVersion }}
          BRANCH="chore/artifacthub-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add artifacthub/
          git diff --staged --quiet || git commit -m "chore: add artifacthub metadata for ${VERSION}"
          git push --force origin "${BRANCH}"
          # Open PR only if one doesn't already exist for this branch
          gh pr create \
            --title "chore: add artifacthub metadata for ${VERSION}" \
            --body "Automated PR created by the release workflow for ${VERSION}. Merge to complete the release." \
            --base main \
            --head "${BRANCH}" || true

      - name: Tag and create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git tag ${{ github.event.inputs.nextVersion }}
          git push origin ${{ github.event.inputs.nextVersion }}
          gh release create ${{ github.event.inputs.nextVersion }} \
            --generate-notes \
            --target ${{ github.sha }} \
            dist/main.js \
            ${{ env.TARBALL }}
